{
  "name": "ai-smart-document-assistant-frontend",
  "version": "1.0.0",
  "description": "Frontend for AI Smart Document Assistant",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && cp -r pages css js assets public/ 2>/dev/null || true",
    "start": "python -m http.server 3000"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": "24.x"
  }
}